/**
 * App root component — placeholder.
 *
 * Full router + provider wiring implemented in story-5-2.
 * Story 1.10 — scaffold (smoke test target).
 */
import React from "react";

export default function App() {
  return (
    <div className="min-h-screen bg-background">
      <p className="sr-only">Choka Dashboard loading…</p>
    </div>
  );
}
